# Identificación del proyecto en SonarCloud
sonar.projectKey=MrDyslexia_Fracturas-de-cadera
sonar.organization=mrdyslexia

# Nombre y versión (opcional pero recomendado)
sonar.projectName=Fracturas de Cadera
sonar.projectVersion=1.0

# Codificación
sonar.sourceEncoding=UTF-8

# Carpetas a analizar (todas tus fuentes en TSX/TS)
sonar.sources=app,components,contexts,data,hooks,types,utils

# Incluir solo extensiones relevantes
sonar.inclusions=**/*.ts,**/*.tsx

# Excluir archivos innecesarios (builds, configs, etc.)
sonar.exclusions=**/node_modules/**,**/.next/**,**/build/**,**/dist/**,**/*.config.js,**/*.config.ts

# Rutas a los tests (si más adelante agregas Jest u otro framework)
#sonar.tests=tests
#sonar.test.inclusions=**/*.test.ts,**/*.test.tsx,**/*.spec.ts,**/*.spec.tsx
